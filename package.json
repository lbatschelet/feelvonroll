{
  "name": "feelvonroll",
  "private": true,
  "description": "Umbrella repository: git submodules feelvonroll-webapp, feelvonroll-admin, feelvonroll-api. Root package manifest keeps optional GLB toolchain deps here so deployments do not auto-select model-pipeline/campus/tools. For deployments with pnpm workspaces and brands, merge branch chore/monorepo.",
  "type": "module",
  "packageManager": "pnpm@10.33.2",
  "scripts": {
    "build": "node -e \"console.log('[feelvonroll root] Nothing to compile at umbrella root — use submodule repos or branch chore/monorepo.'); process.exit(0)\""
  },
  "devDependencies": {
    "@gltf-transform/core": "^4.2.1",
    "@gltf-transform/extensions": "^4.2.1",
    "@gltf-transform/functions": "^4.2.1",
    "meshoptimizer": "^0.24.0"
  }
}
